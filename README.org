#+begin_quote
*⚠ Beta* — 项目正处于活跃开发阶段 / This project is under active development.
#+end_quote

* Brale — Break a Leg

交易如同一场演出。/Break a leg/ — 祝你好运。

AI 驱动的多智能体量化交易引擎。
[[https://brale.club][brale.club]]

*语言 / Language:* [[#中文版][简体中文]] | [[#english-version][English]]

-----

* 中文版
:PROPERTIES:
:CUSTOM_ID: 中文版
:END:

** 什么是 Brale

Brale 是一个量化交易引擎，通过多个大语言模型智能体（LLM Agent）分析市场行情，并结合确定性的 Go 规则引擎来强制执行风控约束。每个决策周期经过六个阶段：

| 阶段 | 职责 |
|------+------|
| *观测（Observe）* | 采集 K 线、指标、市场结构、衍生品与情绪数据 |
| *决策（Decide）* | 三个独立智能体并行分析；共识规则筛选信号 |
| *规划（Plan）* | 生成风控计划 — 入场价、止损、止盈、仓位大小 |
| *执行（Execute）* | 通过 Freqtrade 下单 |
| *监控（Monitor）* | 实时持仓风险追踪与止损调整 |
| *对账（Reconcile）* | 通过 Webhook / 轮询同步 Brale 与交易所状态 |

** 设计理念

- *LLM 提议，代码决断* — 模型建议止盈 / 止损位；Go 代码负责校验价格合法性、杠杆上限和爆仓安全。模型可能产生幻觉，代码会将其拦截。
- *三条独立智能体车道（Agent Lane）* — 指标（Indicator）、结构（Structure）和力学（Mechanics）智能体各自独立运行。共识阈值防止单一模型的误判。
- *Provider 锚定因子（Grounding Anchors）* — 每个 Provider 不仅接收其 Agent 的摘要，还会收到代码提取的结构化锚定数据（跨时间框架对齐、结构突破、力学冲突），用于交叉验证，降低幻觉风险。
- *经验驱动的记忆系统* — 三层记忆系统（工作记忆 Working、情景记忆 Episodic、语义记忆 Semantic）将近期上下文、历史交易复盘和长期规则注入 LLM 提示词。
- *影子指标引擎（Shadow Indicator Engine）* — 内置参考计算引擎与 go-talib 交叉校验，确保指标数据可信。差异检测通过 ~bralectl indicator diff~ 执行。
- *Gate Replay 回测* — 将历史决策通过当前规则集重放，零 LLM 调用开销，用于快速验证规则调优效果。
- *全自动调度* — 决策周期、监控和对账均由内置定时器驱动，无需外部 cron。
- *可插拔执行层* — 当前对接 Freqtrade；接口已解耦，可替换其他执行后端。

** 快速开始

*前置条件：* Docker 与 Docker Compose。本地开发需要 Go 1.25+。

*** 一键部署

#+begin_src bash
curl -fsSL https://raw.githubusercontent.com/laukkw/brale-core/master/scripts/bootstrap.sh | bash
# 或：跳过引导界面，先初始化 .env 并进入 setup 向导
curl -fsSL https://raw.githubusercontent.com/laukkw/brale-core/master/scripts/bootstrap.sh | bash -s -- --no-onboarding --setup
#+end_src

*** 手动部署

#+begin_src bash
git clone --depth 1 https://github.com/laukkw/brale-core.git
cd brale-core
make env-init                                  # 缺失时从 .env.example 创建 .env
make setup                                     # 初始化本地环境，并可选安装 MCP 客户端配置
make init                                      # 推荐首次使用：启动引导界面，访问 127.0.0.1:9992
# 或：编辑好 .env 后直接启动核心栈
make start ENABLE_MCP=1 ENABLE_ONBOARDING=0
#+end_src

*** 常用命令

| 命令 | 说明 |
|------+------|
| =make env-init= | 缺失时复制 =.env.example= 到 =.env= |
| =make setup= | 运行本地 setup 向导（=.env= + 可选 MCP 安装） |
| =make status= | 容器健康检查 |
| =make logs= | 跟踪服务日志 |
| =make mcp-start= | 单独启动 MCP SSE 服务 |
| =make stop= / =make down= | 停止 / 移除容器 |
| =make rebuild= | 重新构建 Brale；若 =ENABLE_MCP=1= 同时重建 =mcp-sse= |

*** Docker 服务

| 服务 | 端口 | 说明 |
|------+------+------|
| onboarding | =127.0.0.1:9992= | 配置引导界面（=make init= 或 =ENABLE_ONBOARDING=1= 时启动） |
| freqtrade | =127.0.0.1:8080= | 交易执行 |
| brale | =127.0.0.1:9991= | 运行时 API / Webhook |
| mcp-sse | =127.0.0.1:8765= | MCP SSE 端点（=make mcp-start= 或 =ENABLE_MCP=1=） |

** 命令行工具 — bralectl

=bralectl= 提供运行时管理、离线分析与本地 setup 三大类功能，共 12 个命令组。

| 命令组 | 说明 |
|--------+------|
| =setup= | 初始化 =.env=，并可选安装 MCP 客户端配置 |
| =add-symbol= | 从现有币种模板生成配置并注册新币种 |
| =observe= | 触发观测、查看报告 |
| =decision= | 查询最新及历史决策 |
| =position= | 查看持仓与已平仓记录 |
| =schedule= | 启用 / 禁用 / 查看调度器状态 |
| =config= | 查看 / 校验配置 |
| =backtest= | Gate Replay — 用当前规则重放历史决策（零 LLM 开销） |
| =indicator= | 交叉对比 go-talib 与参考指标引擎 |
| =memory= | 管理语义规则、查看交易复盘 |
| =mcp= | 启动 MCP 服务器或安装到 AI 工具 |
| =llm= | 测试 LLM 端点连通性 |

详见 [[file:docs/cli-reference.org][CLI 参考手册]]。

** MCP 集成

brale-core 通过 [[https://modelcontextprotocol.io][模型上下文协议（Model Context Protocol）]] 暴露 8 个只读分析工具：

#+begin_src bash
make setup                                    # 交互式初始化 .env + 可选安装 MCP
bralectl mcp install --target opencode        # 自动配置 OpenCode
bralectl mcp serve --mode sse --addr 0.0.0.0:8765  # 远程 SSE 模式
#+end_src

兼容 Claude Code、Claude Desktop、OpenCode、Codex、Cursor 及任何支持 MCP 的智能体。
提供 4 个资源 URI 和 2 个提示词模板。
详见 [[file:docs/mcp-integration.org][MCP 集成指南]]。

** 配置体系

四层 TOML 配置层级：

| 层级 | 路径 | 范围 |
|------+------+------|
| 系统 | =configs/system.toml= | 日志、数据库、LLM 端点、调度、通知 |
| 品种索引 | =configs/symbols-index.toml= | 品种名称到配置 / 策略文件的映射 |
| 品种 | =configs/symbols/*.toml= | 单品种：周期、智能体、指标、记忆、共识 |
| 策略 | =configs/strategies/*.toml= | 单品种：风控参数、Gate/Sieve 规则、止盈止损策略 |

详见 [[file:docs/configuration.org][配置指南]]。

** 文档

| 文档 | 内容 |
|------+------|
| [[file:docs/configuration.org][配置]] | TOML 配置层级、指标参数、添加品种 |
| [[file:docs/trading-logic.org][交易逻辑]] | 入场模式、Provider 锚定因子、Trace 持久化、仓位管理 |
| [[file:docs/runtime-api.org][运行时 API]] | REST 端点、Webhook、通知、存储 |
| [[file:docs/cli-reference.org][CLI 参考]] | 完整的 =bralectl= 命令参考 |
| [[file:docs/mcp-integration.org][MCP 集成]] | 安装、工具、资源、提示词、Docker 部署 |

** 扩展指引

| 目标 | 从哪里开始 |
|------+------------|
| 添加交易品种 | =bralectl add-symbol --symbol XXX= 或 [[file:docs/configuration.org][配置文档]] |
| 调整风控 | =configs/strategies/*.toml= |
| 调整智能体 | =configs/symbols/*.toml= + LLM 环境变量 |
| 对接其他交易所 | =internal/execution= 接口 |
| 自定义监控 | 运行时 API（=:9991=）与 Webhook 端点 |

** LLM Token 用量参考

单次决策周期 = 所有 Agent + Provider + 风控 LLM 调用的 Token 总和：

| 配置 | 输入 / 周期 | 输出 / 周期 | 日估算 |
|------+-------------+-------------+--------|
| 1 品种 × 3 时间框架 × 1 小时周期 | 11 K–12 K | 1.1 K–1.2 K | ≈ 300 K |
| 1 品种 × 3 时间框架 × 15 分钟周期 | 11 K–12 K | 1.1 K–1.2 K | ≈ 1.1 M–1.2 M |

多品种时按比例线性增长。启用记忆系统后，每周期额外增加约 500–1 K Token（工作记忆 / 情景记忆 / 语义记忆上下文注入）。

** 许可证

GNU Affero 通用公共许可证 v3.0（AGPL-3.0-only）。详见 [[file:LICENSE][LICENSE]]。

** 支持

#+begin_example
ETH:  0x4b4C8041A8154a5AfB87E5589Ea292123E098267
BTC:  bc1q6ztlu88dljsn07l6x3xydfa64n0n8pjc38v75a
SOL:  G6vjnENdVoZQv2k9Tx97Z1MqPEEPxNeyuJ1Z8NqSgC9u
TRON: TTUzmkqSSjuXrwUkFFmaoNbnQC4gSCNnvh
#+end_example

** 免责声明

- 本项目仅用于技术研究与流程验证，不构成任何投资建议。
- 数字资产交易波动剧烈，可能导致部分或全部资金损失。
- 用户须自行评估策略、参数及风控措施，并承担全部责任。
- 历史表现、回测结果或示例配置不代表未来收益。
- 投入生产前，请务必在隔离环境中完成充分测试。

-----

* English Version
:PROPERTIES:
:CUSTOM_ID: english-version
:END:

** What Is Brale

Brale is a quantitative trading engine that uses multiple LLM Agents to analyze markets and
a deterministic Go rule engine to enforce risk constraints. Each decision cycle flows through
six stages:

| Stage | Role |
|-------+------|
| *Observe* | Collect candlesticks, indicators, market structure, derivatives and sentiment |
| *Decide* | Three independent Agents analyze in parallel; consensus rules filter signals |
| *Plan* | Generate risk plan — entry, stop-loss, take-profit, position size |
| *Execute* | Place orders via Freqtrade |
| *Monitor* | Real-time position risk tracking and stop adjustment |
| *Reconcile* | Sync state between Brale and the exchange via webhooks/polling |

** Design Principles

- *LLM proposes, code disposes* — models suggest TP/SL levels; Go code enforces price validity,
  leverage caps, and liquidation safety. The model may hallucinate; the code catches it.
- *Three independent Agent lanes* — indicator, structure, and mechanics Agents run separately.
  Consensus thresholds prevent single-model misjudgment.
- *Provider grounding anchors* — each Provider receives its Agent summary *plus* code-extracted
  structured anchors (cross-timeframe alignment, structure breaks, mechanics conflicts) for
  cross-verification, reducing hallucination risk.
- *Experience-driven memory* — three-layer memory system (working, episodic, semantic) feeds
  recent context, historical trade reflections, and long-term rules into LLM prompts.
- *Shadow indicator engine* — built-in reference computation engine cross-validates against go-talib,
  ensuring indicator data integrity. Run ~bralectl indicator diff~ for diff checks.
- *Gate Replay backtesting* — replay historical decisions through the current rule set with zero
  LLM calls, enabling rapid rule-tuning validation.
- *Fully automated scheduling* — decision cycles, monitoring, and reconciliation run on built-in
  timers. No external cron required.
- *Swappable execution* — currently wired to Freqtrade; the interface is decoupled.

** Quick Start

*Prerequisites:* Docker and Docker Compose. Local development requires Go 1.25+.

*** One-Click

#+begin_src bash
curl -fsSL https://raw.githubusercontent.com/laukkw/brale-core/master/scripts/bootstrap.sh | bash
#+end_src

*** Manual

#+begin_src bash
git clone --depth 1 https://github.com/laukkw/brale-core.git
cd brale-core
make env-init                                  # create .env from .env.example when missing
make setup                                     # initialize local setup and optionally install MCP client config
make init                                      # recommended for first-time setup: launch the UI at 127.0.0.1:9992
# or: edit .env first, then start the core stack directly
make start ENABLE_MCP=1 ENABLE_ONBOARDING=0
#+end_src

*** Common Commands

| Command | Description |
|---------+-------------|
| =make env-init= | Copy =.env.example= to =.env= when missing |
| =make setup= | Run the local setup wizard (=.env= + optional MCP install) |
| =make status= | Container health check |
| =make logs= | Tail service logs |
| =make mcp-start= | Start only the MCP SSE service |
| =make stop= / =make down= | Stop / remove containers |
| =make rebuild= | Rebuild Brale; also rebuild =mcp-sse= when =ENABLE_MCP=1= |

*** Docker Services

| Service | Port | Role |
|---------+------+------|
| onboarding | =127.0.0.1:9992= | Setup UI (started by =make init= or =ENABLE_ONBOARDING=1=) |
| freqtrade | =127.0.0.1:8080= | Trade execution |
| brale | =127.0.0.1:9991= | Runtime API / webhook |
| mcp-sse | =127.0.0.1:8765= | MCP SSE endpoint (=make mcp-start= or =ENABLE_MCP=1=) |

** CLI Tool — bralectl

=bralectl= covers live runtime management, offline analysis, and local setup across 12 command groups.

| Group | Description |
|-------+-------------|
| =setup= | Initialize =.env= and optionally install MCP client config |
| =add-symbol= | Generate configs from an existing symbol template |
| =observe= | Trigger observations, view reports |
| =decision= | Query latest and historical decisions |
| =position= | View open and closed positions |
| =schedule= | Enable / disable / status of the scheduler |
| =config= | View / validate configuration |
| =backtest= | Gate Replay — replay decisions through current rules (zero LLM cost) |
| =indicator= | Cross-check go-talib vs reference indicator engine |
| =memory= | Manage semantic rules and view trade reflections |
| =mcp= | Start MCP server or install into AI tools |
| =llm= | Test LLM endpoint connectivity |

See [[file:docs/cli-reference.org][CLI Reference]] for complete usage.

** MCP Integration

brale-core exposes 8 read-only analysis tools via the
[[https://modelcontextprotocol.io][Model Context Protocol]]:

#+begin_src bash
make setup                                    # interactive .env + MCP bootstrap
bralectl mcp install --target opencode        # auto-configure OpenCode
bralectl mcp serve --mode sse --addr 0.0.0.0:8765  # remote SSE mode
#+end_src

Compatible with Claude Code, Claude Desktop, OpenCode, Codex, Cursor, and any MCP-capable Agent.
Provides 4 resource URIs and 2 prompt templates.
See [[file:docs/mcp-integration.org][MCP Integration Guide]].

** Configuration

Four-layer TOML hierarchy:

| Layer | Path | Scope |
|-------+------+-------|
| System | =configs/system.toml= | Logging, DB, LLM endpoints, scheduling, notifications |
| Symbol Index | =configs/symbols-index.toml= | Maps symbols to config/strategy files |
| Symbol | =configs/symbols/*.toml= | Per-symbol: intervals, agents, indicators, memory, consensus |
| Strategy | =configs/strategies/*.toml= | Per-symbol: risk params, gate/sieve rules, TP/SL policy |

See [[file:docs/configuration.org][Configuration Guide]] for details.

** Documentation

| Document | Content |
|----------+---------|
| [[file:docs/configuration.org][Configuration]] | TOML config layers, indicator parameters, adding symbols |
| [[file:docs/trading-logic.org][Trading Logic]] | Entry modes, Provider anchors, trace persistence, position sizing |
| [[file:docs/runtime-api.org][Runtime API]] | REST endpoints, webhooks, notifications, storage |
| [[file:docs/cli-reference.org][CLI Reference]] | Complete =bralectl= command reference |
| [[file:docs/mcp-integration.org][MCP Integration]] | Setup, tools, resources, prompts, Docker deployment |

** Extension Points

| Goal | Where to Start |
|------+----------------|
| Add a symbol | =bralectl add-symbol --symbol XXX= or [[file:docs/configuration.org][config docs]] |
| Tune risk | =configs/strategies/*.toml= |
| Adjust Agents | =configs/symbols/*.toml= + LLM env vars |
| Another exchange | =internal/execution= interface |
| Custom monitoring | Runtime API (=:9991=) and webhook endpoints |

** LLM Token Usage

Single decision cycle = all Agent + Provider + risk LLM calls combined:

| Configuration | Input / cycle | Output / cycle | Daily estimate |
|---------------+---------------+----------------+----------------|
| 1 symbol × 3 tf × 1 h interval | 11 K–12 K | 1.1 K–1.2 K | ≈ 300 K |
| 1 symbol × 3 tf × 15 min interval | 11 K–12 K | 1.1 K–1.2 K | ≈ 1.1 M–1.2 M |

Scale linearly for multiple symbols. With memory enabled, add ~500–1 K tokens/cycle for
working/episodic/semantic context injection.

** License

GNU Affero General Public License v3.0 only (AGPL-3.0-only). See [[file:LICENSE][LICENSE]].

** Support

#+begin_example
ETH:  0x4b4C8041A8154a5AfB87E5589Ea292123E098267
BTC:  bc1q6ztlu88dljsn07l6x3xydfa64n0n8pjc38v75a
SOL:  G6vjnENdVoZQv2k9Tx97Z1MqPEEPxNeyuJ1Z8NqSgC9u
TRON: TTUzmkqSSjuXrwUkFFmaoNbnQC4gSCNnvh
#+end_example

** Disclaimer

- This project is for technical research and workflow validation only; it does not constitute
  investment advice.
- Digital asset trading is highly volatile and may result in partial or total loss of funds.
- Users bear full responsibility for evaluating strategies, parameters, and risk controls.
- Historical performance, backtest results, or sample configurations do not guarantee future returns.
- Complete thorough testing in an isolated environment before enabling in production.
